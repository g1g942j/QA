{
  "name": "recipe-book-front",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm run ui-tests",
    "unit-tests": "jest",
    "ui-tests": "concurrently --kill-others --success first -n serve,test \"npx --yes serve . -l 52900 --no-port-switching\" \"npx wait-on http://127.0.0.1:52900/index.html && mocha --config .mocharc.cjs\""
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "concurrently": "^9.1.2",
    "jest": "^30.2.0",
    "mocha": "^11.0.1",
    "selenium-webdriver": "^4.27.1",
    "tsx": "^4.19.2",
    "wait-on": "^8.0.1"
  }
}
